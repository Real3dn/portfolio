[build]
  command = "npm run build"
  publish = "dist"

# Ensure screenshots are served as static files
[[redirects]]
  from = "/screenshots/*"
  to = "/screenshots/:splat"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200